---
interface Props {
  title: string
  detail: string
}

const { title, detail } = Astro.props
---

<div class="flex flex-col gap-1">
  <h4 class="text-gray-700 font-semibold">{title}</h4>
  <p class="text-slate-950">
    {detail}
  </p>
</div>
